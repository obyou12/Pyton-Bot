

# it will get a text from container and save it on a varable. 
textFromContainer = 'n/a'

# what kind of GPU you are looking for?
gpuName = 'GeForce RTX 3080'
  
# returns first occurrence of Substring 
result = textFromContainer.find(gpuName)

if(result == True)
    
  
# How to use find() 
if (word.find('pawan') != -1): 
    print ("Contains given substring ") 
else: 
    print ("Doesn't contains given substring") 